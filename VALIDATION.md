# Έλεγχος σχολικής έκδοσης

## Αυτοματοποιημένος έλεγχος

Η έκδοση ελέγχεται με:

```bash
node scripts/validate-release.mjs
```

Η ίδια εντολή εκτελείται από το workflow `.github/workflows/validate-release.yml` σε κάθε pull request. Το workflow χρησιμοποιεί Node.js 24, ενεργοποιεί `pipefail`, καταγράφει stdout και stderr και ανεβάζει τα αρχεία `validation.log` και `validation-report.json` ως artifact.

## Επιτυχές αποτέλεσμα

GitHub Actions run **26**, commit `6a58ee46af48edea3cf4e022675671a1659470cf`: **PASS**.

```text
AGVL school-corpus validation: PASS
School entries: 11.530
School sources: 7
Decoded app source: 75.800 characters
Corpus SHA-256: 73f7e64250f2427c05d5c5704beb96dba9e55097136ea36dede5c3f48a211742
App SHA-256: df0908e07c3cca4292c9bb5fc49456d97f99bbd39672367abaff58f15b396e29
```

Το αποσυμπιεσμένο app bundle έχει μέγεθος **83.233 bytes**. Η διαφορά από τους 75.800 χαρακτήρες οφείλεται στην κωδικοποίηση UTF-8 του ελληνικού κειμένου.

## Έλεγχοι που εκτελούνται

### Manifest και αρχεία

- δημιουργία του `window.SCHOOL_CORPUS`·
- ακριβώς 7 σχολικές πηγές·
- συνέπεια `packedPartCount` και `packedPartFiles`·
- συνέπεια `appPartCount` και `appPartFiles`·
- ύπαρξη όλων των δηλωμένων shards.

### Σχολικό σώμα

- εκτέλεση των επτά shard wrappers σε απομονωμένο VM context·
- ακριβώς ένα έγκυρο string ανά shard·
- ένωση standard base64·
- gzip αποσυμπίεση·
- SHA-256 του αποσυμπιεσμένου payload·
- έγκυρο JSON·
- ακριβώς 11.530 εγγραφές·
- 11.530 μοναδικά κανονικοποιημένα κλειδιά·
- έγκυρο bitmask επτά βιβλίων για κάθε εγγραφή·
- παρουσία ελληνικών χαρακτήρων.

### Κώδικας εφαρμογής

- ένωση τεσσάρων app shards·
- standard base64 και gzip·
- SHA-256 του αποσυμπιεσμένου κώδικα·
- JavaScript syntax check με `vm.Script`·
- παρουσία των βασικών markers αναγνώρισης, πλήρους κλίσης και παιχνιδιού.

### Διεπαφή και καθαρισμός

- απαιτούμενα DOM ids·
- ύπαρξη όλων των scripts που αναφέρει το `index.html`·
- σωστή σειρά manifest, compatibility bootstraps και loader·
- δομικός έλεγχος του CSS·
- απουσία παλαιών viewer shards και διεφθαρμένων ή πειραματικών corpus payloads.

## Αιτία της προηγούμενης αποτυχίας

Οι αποτυχημένοι έλεγχοι που εμφανίστηκαν αρχικά στο pull request δεν οφείλονταν στην προειδοποίηση για τη Node.js. Η πραγματική αιτία ήταν αλλοίωση ενός υπερμεγέθους base64 shard κατά προηγούμενο upload. Η gzip αποσυμπίεση σταματούσε με `incorrect data check`.

Η διόρθωση περιλάμβανε:

1. αντικατάσταση των δύο παλαιών μεγάλων corpus payloads με μικρά compatibility bootstraps·
2. επαναφορά του byte-verified σχολικού σώματος·
3. κατάτμηση του προβληματικού τμήματος 05 σε `05a` και `05b`·
4. σύγκριση των Git blob SHA των νέων shards με τα τοπικά επαληθευμένα αρχεία·
5. προσθήκη runtime SHA-256 πριν από την εκτέλεση·
6. προσθήκη validator που απορρίπτει ρητά το παλαιό διεφθαρμένο αρχείο.

## Χειροκίνητη δοκιμή

Για τοπική δοκιμή:

```bash
python -m http.server 8000
```

Στη συνέχεια ελέγχονται τουλάχιστον:

1. φόρτωση χωρίς μήνυμα σφάλματος·
2. αναγνώριση τύπων όπως `ἔλαβον`, `εἴη` και `πεποιηκώς`·
3. μετάβαση στην «Πλήρη κλίση» και επιλογή σχολικού λήμματος·
4. εμφάνιση προσωπικών τύπων, απαρεμφάτων και μετοχών·
5. δημιουργία νέας κάρτας στην «Εξάσκηση»·
6. φίλτρο σχολικού εγχειριδίου και ομαδική λειτουργία.
