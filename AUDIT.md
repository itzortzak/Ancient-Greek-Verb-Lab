# Audit

The previously unresolved problematic entries were removed from the shipped package in this build. See `INTEGRATION_NOTES.md` for the cleanup summary and the current source-integration model.
